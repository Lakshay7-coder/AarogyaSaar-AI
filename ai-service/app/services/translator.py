def translate_text(
    text,
    source_language,
    target_language
):

    if source_language == target_language:
        return text

    # Prototype fallback.
    # Replace this function with an actual
    # translation API/model during deployment.

    return text