import { BrowserRouter, Navigate, Route, Routes } from "react-router-dom";
import { AuthProvider, useAuth } from "./context/AuthContext";
import { CaseProvider } from "./context/CaseContext";
import Landing from "./pages/Landing"; import PatientSummary from "./pages/PatientSummary"; import Login from "./pages/Login"; import Register from "./pages/Register"; import Consent from "./pages/Consent"; import PatientDashboard from "./pages/PatientDashboard"; import CaseTaking from "./pages/CaseTaking"; import Documents from "./pages/Documents"; import Timeline from "./pages/Timeline"; import DoctorDashboard from "./pages/DoctorDashboard"; import PatientCases from "./pages/PatientCases"; import CaseDetails from "./pages/CaseDetails";

function Guard({ children, role }) { const { user, loading } = useAuth(); if (loading) return <div className="app-loading">Loading…</div>; if (!user) return <Navigate to="/login" replace/>; if (role && user.role !== role) return <Navigate to={user.role === "doctor" ? "/doctor" : "/patient/dashboard"} replace/>; return children; }
function App(){ return <BrowserRouter><AuthProvider><CaseProvider><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route path="/consent" element={<Guard role="patient"><Consent/></Guard>}/><Route path="/patient/dashboard" element={<Guard role="patient"><PatientDashboard/></Guard>}/><Route path="/case/:caseId" element={<Guard role="patient"><CaseTaking/></Guard>}/><Route path="/case/:caseId/documents" element={<Guard role="patient"><Documents/></Guard>}/><Route path="/case/:caseId/timeline" element={<Guard role="patient"><Timeline/></Guard>}/><Route path="/case/:caseId/summary" element={<Guard role="patient"><PatientSummary/></Guard>}/><Route path="/doctor" element={<Guard role="doctor"><DoctorDashboard/></Guard>}/><Route path="/doctor/cases" element={<Guard role="doctor"><PatientCases/></Guard>}/><Route path="/doctor/cases/:caseId" element={<Guard role="doctor"><CaseDetails/></Guard>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></CaseProvider></AuthProvider></BrowserRouter> }
export default App;
