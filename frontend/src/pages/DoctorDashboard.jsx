import { useEffect, useState } from "react"; import { Clock3, ShieldAlert, CheckCircle2, Users, Sparkles } from "lucide-react"; import { Link } from "react-router-dom"; import Navbar from "../components/common/Navbar"; import Sidebar from "../components/common/Sidebar"; import StatCard from "../components/doctor/StatCard"; import CaseCard from "../components/doctor/CaseCard"; import api from "../services/api";
export default function DoctorDashboard(){const [data,setData]=useState({stats:{total:0,awaitingReview:0,verified:0,redFlags:0},cases:[]});const [error,setError]=useState("");useEffect(()=>{api.get("/doctor/dashboard").then(r=>setData(r.data)).catch(e=>setError(e.response?.data?.message||"Unable to load doctor dashboard."))},[]);const patients=(data.cases||[]).map(c=>({id:c._id,name:c.patient?.name||"Unknown",age:c.patientInfo?.age||"—",gender:c.patientInfo?.gender||"—",complaint:c.extractedSymptoms?.[0]?.name||"Not specified",status:c.status.replaceAll("_"," "),priority:c.redFlags?.length?"High":"Normal",completeness:c.completeness||0,time:new Date(c.updatedAt||c.createdAt).toLocaleString()}));return <div className="app-shell"><Navbar/><div className="app-body"><Sidebar doctor/><main className="doctor-main"><div className="doctor-heading"><div><span className="form-eyebrow">CLINICAL COMMAND CENTER</span><h1>Doctor dashboard</h1><p>Review AI-assisted patient cases and focus on clinical decisions.</p></div><div className="doctor-ai-status"><div><Sparkles size={16}/></div><span>AI systems operational</span></div></div>{error&&<div className="form-error">{error}</div>}<div className="stats-grid"><StatCard icon={<Users size={20}/>} label="Active Cases" value={data.stats.total} trend="Review queue"/><StatCard icon={<Clock3 size={20}/>} label="Awaiting Review" value={data.stats.awaitingReview} trend="Needs attention"/><StatCard icon={<ShieldAlert size={20}/>} label="Red Flags" value={data.stats.redFlags} trend="Priority cases" danger/><StatCard icon={<CheckCircle2 size={20}/>} label="Verified" value={data.stats.verified} trend="Completed"/></div><div className="doctor-content-grid"><section className="doctor-cases-panel"><div className="section-heading"><div><span>PRIORITY QUEUE</span><h2>Patient cases</h2></div><Link to="/doctor/cases">View all</Link></div><div className="doctor-case-list">{patients.length?patients.map(p=><CaseCard key={p.id} patient={p}/>):<div className="empty-state">No patient cases are awaiting review.</div>}</div></section><aside className="doctor-side-panel"><div className="doctor-ai-card"><div className="ai-card-orb"><Sparkles size={20}/></div><span>AAROGYASAAR AI</span><h3>Documentation, intelligently assisted.</h3><p>AI structures patient narratives, identifies missing information and prepares summaries for review.</p><div className="ai-capability"><span>✓</span>Natural language understanding</div><div className="ai-capability"><span>✓</span>Adaptive questioning</div><div className="ai-capability"><span>✓</span>Red-flag support</div><div className="ai-capability"><span>✓</span>Clinical summary</div></div><div className="doctor-principle-card"><ShieldAlert size={20}/><div><strong>Human-in-the-loop</strong><p>AI recommendations never replace professional clinical judgement.</p></div></div></aside></div></main></div></div>}
